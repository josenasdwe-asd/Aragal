/**
 * Shared constant for the admin token localStorage key.
 * Kept in a separate file (without `server-only`) so client components
 * can import it.
 */
export const ADMIN_TOKEN_STORAGE_KEY = "aragal_admin_token";
